### Requirements

* openai>=0.27.0

### How to use

* Install package
  ```
  pip install openai
  ```
* Replace with your secret key
* Run the script
  ```python
  python chatUI.pyw
  ```

### API key

* get key from https://platform.openai.com/
* 右上角点击头像，view API keys
